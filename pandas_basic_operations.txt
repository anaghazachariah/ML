#To read data using pandas to a variable df

    df = pd.read_csv(file_path)

#To get all columns in dataset as list

    column_names=list(df.columns.values)

#To get m to n rows

    sub_df=df[m:n]

#To get value of cell in p'th row and having column name 'COL' and store it in variable n

    n=df.iloc[p]['COL']
        or
    n=df['COL'][p]
        or
    n=df.loc[p,'COL]
        or
    n=df.COL.iloc[p]

#To get all values in column named 'COL' as a list to variable n

    n=df['COL'].tolist()

#To get a subser of dataframe with single column names 'COL'

    sub_df=df.COL
        or
    sub_df=df['COL']

#To get average of values in column named 'COL' to variable n

    n=df["COL"].mean()
        or
    n=df.COL.mean()

#To get unique values in column as a list

    unique_vals=df['COL'].unique()
        or
    unique_vals=df.COL.unique()

#To get a subset of dataset with unique values of column 'COL' and how often they occur

    sub_df=df.COL.value_counts()
        or
    sub_df=df.groupby('COL).COL.count()
    
#To get summary of data

    df.describe()

#To get summary of each column 'COL'

    df.COL.describe()

#To get first 5 entries in data frame

    df.head()

#To drop all missing values in data

    df=df.dropna(axis=0)

#To replace all missing values with 'missing'

    df.fillna('missing', inplace=True)

#To select rows with missing values in COL

    sub_df=df[pd.isnull(df.COL)]

#To fill missing values of a column

    sub_df=df.COL.fillna('missing')

#To get size of dataframe

    df.shape

#For not having a dedicated column for index

    df=pd.read_csv('FILE_PATH',index_col=0)

#To create a dataframe with column 'Yes' having values 50 and 21 and column 'np' having 131 and 2

    df=pd.DataFrame({'Yes': [50, 21], 'No': [131, 2]})

#To specify index

    df=pd.DataFrame({'Bob': ['I liked it.', 'It was awful.'], 'Sue': ['Pretty good.', 'Bland.']},index=['Product A', 'Product B'])

#To select n'th row 

    sub_df=df.iloc[n]

#To get n'th column

    sub_df=df.iloc[:,0]

#To get values of m'th column of first n rows

    sub_df=df.iloc[:n,m]

#To get values of m'th column of p to q rows

    sub_df=df.iloc[p:q,m]

#To get values of m'th column for a given list of rows [0,1,2,3]

    sub_df==df.iloc[[0,1,2,3],m]

#To get last 5 elements in dataset

    sub_df=df.iloc[-5:]

#To get values of list of columns ['P','Q','R]

    sub_df=df.loc[:,['P','Q','R]]

#To make given column as index

    df..set_index('COL')

#To get rows with column 'COL' having value 'm'

    sub_df=df.loc[df.COL == 'm']

#To get rows with column 'COL1' having value 'm' and 'COL2' having value 'n' 

    sub_df=df.loc[(df.COL1 == 'm') & (df.COL2=='n)]

#To get rows with column 'COL' have value in ['P','Q','R']

    sub_df=df.loc[df.COL.isin(['P','Q','R'])]

#To get rows with values of COL is not null  

    sub_df=df.loc[df.COL.notnull()]

#To assign a value for colum COL

    df['COL']=value

#To assign elements of list as elemnts of column 'COL'

    df['COL']=[1,2,3]

#To subtract x  for all elements in the column named COL without modifying orginal data

    sub_df=df.COL.map(lambda n:n -x)
        or
    def subtracts(row):
        row.COL=row.COL-x
        return row
    sub_df=df.apply(subtracts,axis='columns')
        or
    sub_df=df.COL-x

#To concatinate a string with all values in column

    sub_df=df.COL+'new_string'

#To concatinate another column values with current column

    sub_df=df.COL1+"-"+df.COL2

#To copy dataframe to another dataframe

    copied_df=df

#To calculate median

    calculated_median=df.COL.median()

#To calculate unique value in COL1 and to get minimum value of COL2 correspond to each gropu in COL1

    sub_df=df.gropuby('COL1).C0L2.min()  # cheapest COL2 in each COL1 value category

#To rename COL1 as COL2

    df.rename(columns={'COL1':'COL2'})

#To rename 0th element as firth and 1'st elemnt as second in index

    df.rename(index={0:'first',1:'second'})

#To concatinate 2 datasets

    df3=pd.concatinate([df1,df2])

#Inner join between 2 datasets on column COL

    df3=pd.merge(df1,df2,on='COL',how='inner')
        or
     df3=pd.merge(df1, df2,  how='inner', left_on=['df1_COL1','df1_COL2'], right_on = ['df1_COL1','df2_COL2'])

#Left join between 2 datasets on column COL

    df3=pd.merge(df1,df2,on='COL',how='left')
        or
     df3=pd.merge(df1, df2,  how='left', left_on=['df1_COL1','df1_COL2'], right_on = ['df1_COL1','df2_COL2'])

#Right join between 2 datasets on column COL

    df3=pd.merge(df1,df2,on='COL',how='right')
        or
    df3=pd.merge(df1, df2,  how='right', left_on=['df1_COL1','df1_COL2'], right_on = ['df1_COL1','df2_COL2'])

#To get type of COL

    df.COL.dtype

#To get type of all columns

    df.types

#To convert a column type to float64

    df.COL.astype('float64')

#To replace value x with y 

    df.COL.replace('x','y')

#To reset dataset back to orginal index

    sub_df=df.reset_index()

#To sort dataset based on COL

    sub_df=df.sort_values(by='COL')

#To sort dataset based on COL in descending order

    sub_df=df.sort_values(by='COL', ascending=False)

#To sort index

    sub_df=df.sort_index()


#To sort dataset by more than 1 column

    df.sort_values(by=['COL1','COL2]) #first groupby col1 and then sort each group by COL2

#To save dataset

    df.to_csv('file_name.csv')

#To convert values in COL to lower case 

    df['COL']=df['COL'].str.lower()

#To remove starting and ending white spaces in column values

    df['COL']=df['COL'].str.strip()

#To scale data in orginal_data, between 0 and 1

    scaled_data=minmax_scaling(orginal_data_list,columns=[0])